## 开发需求描述

你是一个资深Java架构师，熟悉Netty，SpringBoot等主流框架

1. 我要用netty开发一个物联网设备网络服务器，支持多种设备协议处理。采用Maven作为管理工具

2. 尽可能采用如下架构：分包、模块化、分层架构、插件化
3. 插件化可以采用SPI这种机制来进行处理
4. 尽可能使用动态化配置，例如线程池，消息队列类型
5. 减少类之间的强耦合，职责分离。
6. 对资源的处理要合理，要支持优雅停机，防止内存泄露
7. 实现应用层的可靠传输
8. 支持单机和分布式部署
9. 分布式部署下，采用恰当的技术，确保请求路由到合适的服务器节点
10. 目前我个人的设计包括如下组件，属于有缺陷的：
    1. Boostrapper：服务器启动器，定义服务器端口，负责服务的初始化和销毁操作
    2. Listener：定义Netty服务器
    3. SessionProvider：Session注册中心
    4. Session：设备连接会话
    5. MessageProcessor：处理请求，包括处理上行和下行请求的核心执行器
    6. CommandExecutor：命令执行器，包括上行和下行命令的执行器，以及很多详细的命令执行器实现类
    7. MessageForwarder：转发业务消息dto到业务系统，上行消息转发器
    8. DownlinkController：下行命令表现层接口，接收业务系统的调用

3. 尽可能的预留扩展点，灵活的扩展，对扩展开放，包括不限于：
   1. 扩展支持的协议，支持传输层tcp, udp协议，应用层要支持自定义二进制协议、mqtt、modbus等
   2. 扩展Listener，可以根据不同协议，创建不同端口的服务器监听器
   3. Boostrap：服务器启动器，支持多个启动器实例
   4. 扩展下行命令的接收，支持不同渠道，监听队列、表现层controller异步调用
   5. 扩展上行消息转发器，要支持不同渠道，REST，MQ，gRPC等多种方式
   6. 扩展channelInitializer，处理不同格式的报文，例如TXT，JSON，BINARY
   7. 扩展分布式部署下的服务发现机制，支持nacos, etcd, redis, zookeeper
4. 给出详细的设计方案
5. 给出Maven不同的包，不同的maven模块的细节，模块内组件细节
6. 继续与我进行详细讨论，直到给出我满意的设计方案